# Backend API base URL for production.
# In local dev leave this empty — the Vite proxy handles /api/* → localhost:8000.
# On Cloudflare Pages set this to your deployed backend URL, e.g.:
#   VITE_API_BASE_URL=https://api.yourdomain.com
VITE_API_BASE_URL=
